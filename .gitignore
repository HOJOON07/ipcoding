# 모델 파일 — 커밋 금지 (CLAUDE.md 커밋 규칙)
*.bin
*.gguf

# whisper.xcframework — build-xcframework.sh로 재생성 가능한 빌드 산출물 (수 MB)
IpCoding/Vendor/

# macOS
.DS_Store

# Claude Code 개인 설정
.claude/settings.local.json

# Xcode / Swift 빌드 산출물
.build/
DerivedData/
*.xcuserstate
xcuserdata/

# 릴리스 패키징 산출물 (scripts/release.sh)
build/
